//1.Given a string, return a string where for every char in the original, there are two chars.

let myChar = "The"; 
    if(myChar.length("0,  2")){
        console.log(myChar);
           
    }

//2.Sandwich task
let Sandwich1 = "breadjambread"; //find the length and then use that to split the word into 3 words. Then slice and return the last word.  
Sandwich1.length().slice(0, 5);
return("jam"); 

//3. Integers a,b,c
function myFunction (a,b,c){
    if((b-a) == (c-b) ||(a -c) == (b-a) ){
        return true;    
    }else
        return false; 
    }
console.log(myFunction(2,4,6));
console.log(myFunction(4,6,2)); 
console.log(myFunction(4,6,3)); 



//5.Given a string, return true if it ends in "ly".
let endsLy; 
if(lastTwoDigits == "Ly"){
    return true;
}


